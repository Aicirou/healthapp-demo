# Server
PORT=4000
NODE_ENV=development

# Database
# Use 'db' when running via Docker Compose, 'localhost' for local dev
DATABASE_URL=postgres://admin:secret@db:5432/healthapp?sslmode=disable

# JWT
JWT_SECRET=change-this-to-a-secure-random-string
JWT_EXPIRES_IN=1d
